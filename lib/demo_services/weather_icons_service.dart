/*
import '../demo_imports.dart';

class Choice {
  const Choice({required this.icon});

  final IconData icon;
}

const List<Choice> choices = const <Choice>[
  const Choice(icon: WeatherIcons.wiDayRain),
  const Choice(icon: WeatherIcons.wiDaySunny),

];

class ChoiceCard extends StatelessWidget {
  const ChoiceCard({Key ?key, required this.choice}) : super(key: key);
  final Choice choice;

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 45.0,
      height: 45.0,
      child: Card(
        color: Colors.green,
        child: Stack(
          children: [
            Positioned(
                left: 5.0,
                top: 5.0,
                right:5.0,
                child: Icon(
              choice.icon,
              size: 20.0,
              color: Colors.white,
            ))
          ],
        )
      ),
    );
  }
}

 */

